# BasicProcessNode

Minimal C# solution containing one vvvv gamma `ProcessNode`.

## Build

```powershell
dotnet build .\BasicProcessNode.sln
```

## Notes

- Requires the vvvv NuGet feed configured in `NuGet.config`.
- Node discovery requires `[assembly: ImportAsIs]`.
